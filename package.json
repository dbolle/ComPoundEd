{
  "name": "compounded",
  "private": true,
  "version": "1.71.0",
  "description": "A dog-themed PWA that helps kids practice and memorize multiplication tables.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vite build && playwright test",
    "test:only": "playwright test",
    "setup-hooks": "git config core.hooksPath .githooks",
    "gate": "bash scripts/gate.sh"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "sharp": "^0.35.3",
    "vite": "^6.3.5",
    "vite-plugin-pwa": "^0.21.2"
  },
  "overrides": {
    "brace-expansion": "^5.0.8"
  }
}
